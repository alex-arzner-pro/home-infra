# Secrets — never commit
.secrets/
*.env

# MikroTik exports (may contain API tokens/secrets)
*.rsc
*.backup

# Backup artifacts
*.bak
*.orig

# Python bytecode
__pycache__/
*.pyc

# Cat detector: local venv + downloaded model weights (workstation-only, large)
pi-flag-cam/detector/.venv/
*.pt
