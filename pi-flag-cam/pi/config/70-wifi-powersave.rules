# Disable WiFi power save immediately when interface comes up
# Prevents brcmfmac from enabling power save before NetworkManager disables it
ACTION=="add", SUBSYSTEM=="net", KERNEL=="wlan*", RUN+="/usr/sbin/iw dev %k set power_save off"
